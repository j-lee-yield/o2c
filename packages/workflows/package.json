{
  "name": "@o2c/workflows",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "lint": "eslint src/index.ts src/index.test.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@o2c/audit": "workspace:*",
    "@o2c/auth": "workspace:*",
    "@o2c/contracts": "workspace:*",
    "@o2c/domain": "workspace:*"
  },
  "devDependencies": {
    "@o2c/testkit": "workspace:*"
  }
}
